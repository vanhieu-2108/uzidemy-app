export enum ERole {
  ADMIN = 'ADMIN',
  USER = 'USER'
}

export enum EObjectTypeComment {
  LECTURE = 'LECTURE',
  POST = 'POST'
}

export enum EPostStatus {
  PENDING = 'PENDING',
  PUBLIC = 'PUBLIC',
  DELETED = 'DELETED'
}
